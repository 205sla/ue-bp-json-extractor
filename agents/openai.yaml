interface:
  display_name: "UE Asset JSON Extractor"
  short_description: "Extract AI-readable Unreal asset JSON"
  default_prompt: "Use $ue-bp-json-extractor to extract an AI-readable JSON summary from this Unreal Engine asset."
